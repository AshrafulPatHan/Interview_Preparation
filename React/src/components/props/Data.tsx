import MainProps from "./MainProps";

const Data = () => {
    return (
        <div>
            <MainProps name="ashraful" age={18} isStudent={true} />
            {/* <MainProps name="ashraful" age={18} isStudent={true} /> */}
        </div>
    );
};

export default Data;
